(function($) {
var GeoStream = function(config) {
	if (!(config && config.target)) return;
	this.init(config.data);
};

GeoStream.prototype = {
	constructor: GeoStream,
	init: function(data) {
		
	}
})(jQuery);
